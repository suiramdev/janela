{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: core APIs, logging, and native notifications.",
  "windows": ["main"],
  "permissions": ["core:default", "log:default", "notification:default"]
}
