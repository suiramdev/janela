{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: core APIs, dragging the window by its own chrome, holding the window to a theme, logging, notifications, the directory picker, opening a session's directory in Finder or Terminal, checking for and installing the app's own updates and relaunching afterwards.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-theme",
    "log:default",
    "notification:default",
    "dialog:allow-open",
    "opener:default",
    "updater:default",
    "process:allow-restart",
    {
      "identifier": "opener:allow-open-path",
      "allow": [{ "path": "/**", "app": "Terminal" }]
    }
  ]
}
